\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{griger}[2021/12/03 A package providing some stylistic configuration.]

% links
\RequirePackage[unicode, psdextra]{hyperref}

\hypersetup{
    colorlinks = true,
    breaklinks = true,
    allcolors = black,
    linktoc = all
}
